test:
    dummy-flag:
        enabled: true
        type: static
    disabled-flag:
        enabled: false
        type: static
    user-dummy-flag:
        enabled: true
        type: pilot_users
        allowed_users:
            - test_user
            - another_user
